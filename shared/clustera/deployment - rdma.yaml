apiVersion: apps/v1
kind: Deployment
metadata:
  name: onceupon-deployment-rdma
  namespace: llm-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: onceupon-rdma
  template:
    metadata:
      labels:
        app: onceupon-rdma
    spec:
      containers:
      - name: onceupon-container
        image: ik2227/llama2
        env:
        - name: LLAMA_SYS_PROMPT
          value: "Once upon a time,"
        - name: ENABLE_RDMA
          value: "true"
        - name: RXE_IFACE
          value: "rxe1"
        ports:
        - containerPort: 8080
        securityContext:
          privileged: true
        volumeMounts:
        - mountPath: /data/roce
          name: roce-client
        - mountPath: /dev/infiniband
          name: ib
      volumes:
      - name: roce-client
        hostPath:
          path: /shared/roce
      - name: ib
        hostPath:
          path: /dev/infiniband
      nodeSelector:
        supports: rdma
